import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

describe('When I render App component', () => {
    it('works fine', () => {
        expect(1).toBe(1);
    })
})